on:
  push:
  workflow_dispatch:
jobs:
  my_job:
    name: deploy to staging
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make all
      run: |
        ./build_all.sh

    - name: upload
      uses: actions/upload-artifact@v3
      with:
        name: buildz
        path: .
